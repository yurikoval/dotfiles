#!/bin/bash

if [ -f ~/.bundle/config ];
then
 mv ~/.bundle/config ~/.bundle/config_OFF
 echo "Bundle local: on --> OFF"
else
 mv ~/.bundle/config_OFF ~/.bundle/config
 echo "Bundle local: off --> ON"
fi
