---
allowed-tools: Bash(git add:*), Bash(git status:*), Bash(git commit:*)
argument-hint: [plan.md]
description: Implement a plain in [plan.md]
---
Implement plan @$1

- tests must be passing
- Commit progress using git
- group git commits based on change types
- business logic must accompany rspec tests
- maintain readable code
